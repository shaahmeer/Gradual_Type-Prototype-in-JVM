package minigrad.lexer;

public enum TokenType {
    LET,
    PRINT,
    IDENTIFIER,
    TYPE,
    INT_LITERAL,
    STRING_LITERAL,
    COLON,
    EQUALS,
    SEMICOLON,
    PLUS,
    LPAREN,
    RPAREN,
    EOF
}